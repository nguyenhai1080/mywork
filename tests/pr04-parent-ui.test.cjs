const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict'),path=require('node:path');
const app=fs.readFileSync(path.join(__dirname,'../AppJS.html'),'utf8');const start=app.indexOf('function openProjectTaskModal_(parent)');const end=app.indexOf('async function submitProjectTask_',start);let message='';const c=vm.createContext({STATE:{projectDetail:{tasks:[{TaskID:'T1',Status:'COMPLETED'}]}},showToast_:m=>message=m,showModal_:()=>{throw Error('Should not open fallback root task form');}});vm.runInContext(app.slice(start,end),c);vm.runInContext("openProjectTaskModal_('T1')",c);assert.match(message,/active parent/);console.log('PASS completed parent cannot silently fall back to top-level task');